# Core dependencies
pandas>=1.3.0
numpy>=1.20.0
openpyxl>=3.0.0
# Optional: only needed for legacy .xls via pandas (pip install xlrd)
# xlrd>=2.0.1
requests>=2.28.0
gender-guesser>=0.4.0

# Machine learning
pyod>=1.1.0
scikit-learn>=1.0.0

# Web dashboard
flask>=2.0
gunicorn>=21.0

# Pipeline scheduler (automated fetch + analyze jobs)
apscheduler>=3.10.0
sqlalchemy>=1.4.0

# PDF generation
reportlab>=3.6.0

# GeoIP lookups (city-level geo + ASN/datacenter detection)
# Databases: run scripts/download_geoip.py after install
maxminddb>=2.0.0

# Testing
pytest>=7.0.0
